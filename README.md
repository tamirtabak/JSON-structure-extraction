# JSON-structure-extraction
Python script that extract JSON file structure using recursive method and return the JSON structure only without the actual data inside each field. Only the subjects of each node and edge.
